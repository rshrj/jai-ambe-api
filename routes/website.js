const router = require("express").Router();

/*
    => /ads ->  Edit advertisement poster on home page (a simple image of fixed dimensions will be replaced)
*/

router.post("/ads", async () => {});

module.exports = router;
